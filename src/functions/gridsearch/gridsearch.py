from typing import Type

import numpy


def grid_search(combinations: numpy.ndarray = None) -> Type[float]:
    """ Проверка точности результата """
    if combinations is None or not len(combinations):
        return float

    return float


grid_search(None)
